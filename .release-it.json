{
  "hooks": {
    "after:init": ["pnpm run lint"]
  },
  "git": {
    "requireBranch": "master",
    "requireCommits": true,
    "tagName": "${version}",
    "tagAnnotation": "${version}"
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  }
}
