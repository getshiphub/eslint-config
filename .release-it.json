{
  "hooks": {
    "after:init": ["yarn lint"]
  },
  "git": {
    "requireBranch": "master",
    "requireCommits": true,
    "tagName": "${version}",
    "tagAnnotation": "${version}"
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  }
}
